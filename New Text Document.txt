<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>React Developer - Contact</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        :root {
            --primary: #6c63ff;
            --secondary: #ff6584;
            --accent: #63d2ff;
            --light: #f8f9fa;
            --dark: #343a40;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #ffffff;
            color: #333;
            overflow-x: hidden;
        }
        
        .gradient-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(108, 99, 255, 0.05) 0%, rgba(99, 210, 255, 0.05) 100%);
            z-index: -1;
        }
        
        .contact-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            display: flex;
            flex-wrap: wrap;
            gap: 2rem;
            align-items: center;
        }
        
        .illustration-section {
            flex: 1;
            min-width: 300px;
            position: relative;
            height: 500px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .developer-avatar {
            width: 300px;
            height: 300px;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            border-radius: 50%;
            position: relative;
            margin: 0 auto;
            animation: float 6s ease-in-out infinite;
            box-shadow: 0 15px 30px rgba(108, 99, 255, 0.15);
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        
        .developer-avatar::before {
            content: '';
            position: absolute;
            width: 80%;
            height: 80%;
            border-radius: 50%;
            background-color: white;
            z-index: 1;
        }
        
        .avatar-content {
            position: relative;
            z-index: 2;
            text-align: center;
            padding: 1rem;
        }
        
        .avatar-content i {
            font-size: 5rem;
            color: var(--primary);
            margin-bottom: 1rem;
        }
        
        .greeting {
            margin-top: 2rem;
            text-align: center;
        }
        
        .greeting h2 {
            font-size: 2rem;
            color: var(--dark);
            margin-bottom: 0.5rem;
        }
        
        .greeting p {
            color: #666;
            font-size: 1.1rem;
            line-height: 1.6;
        }
        
        .form-section {
            flex: 1;
            min-width: 300px;
            background: white;
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            position: relative;
            overflow: hidden;
        }
        
        .form-section::before {
            content: '';
            position: absolute;
            top: -50px;
            right: -50px;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(108, 99, 255, 0.1) 0%, rgba(108, 99, 255, 0) 70%);
        }
        
        .form-section h2 {
            font-size: 1.8rem;
            color: var(--dark);
            margin-bottom: 1.5rem;
            position: relative;
        }
        
        .form-section h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 50px;
            height: 3px;
            background: linear-gradient(to right, var(--primary), var(--accent));
            border-radius: 3px;
        }
        
        .form-group {
            position: relative;
            margin-bottom: 1.5rem;
        }
        
        .form-control {
            width: 100%;
            padding: 1rem 1rem 1rem 0;
            border: none;
            border-bottom: 2px solid #e0e0e0;
            font-size: 1rem;
            transition: all 0.3s ease;
            background-color: transparent;
        }
        
        .form-control:focus {
            outline: none;
            border-bottom-color: var(--primary);
        }
        
        .form-label {
            position: absolute;
            top: 1rem;
            left: 0;
            color: #999;
            transition: all 0.3s ease;
            pointer-events: none;
        }
        
        .form-control:focus + .form-label,
        .form-control:not(:placeholder-shown) + .form-label {
            top: -0.5rem;
            font-size: 0.8rem;
            color: var(--primary);
        }
        
        .validation-icon {
            position: absolute;
            right: 0;
            top: 1rem;
            opacity: 0;
            transition: all 0.3s ease;
        }
        
        .form-control:valid + .form-label + .validation-icon {
            opacity: 1;
            color: #4CAF50;
        }
        
        .form-control:invalid:not(:placeholder-shown) + .form-label + .validation-icon {
            opacity: 1;
            color: #F44336;
        }
        
        textarea.form-control {
            resize: none;
            min-height: 100px;
        }
        
        .submit-btn {
            background: linear-gradient(to right, var(--primary), var(--accent));
            color: white;
            border: none;
            padding: 1rem 2rem;
            font-size: 1rem;
            border-radius: 50px;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(108, 99, 255, 0.3);
            width: 100%;
            font-weight: 500;
            letter-spacing: 0.5px;
        }
        
        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(108, 99, 255, 0.4);
        }
        
        .ripple {
            position: absolute;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.7);
            transform: scale(0);
            animation: ripple 0.6s linear;
            pointer-events: none;
        }
        
        .contact-options {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin-top: 2rem;
            flex-wrap: wrap;
        }
        
        .contact-option {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            transition: all 0.3s ease;
            position: relative;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        .contact-option:hover {
            transform: translateY(-5px) scale(1.1);
        }
        
        .contact-option::after {
            content: attr(data-tooltip);
            position: absolute;
            bottom: -40px;
            left: 50%;
            transform: translateX(-50%);
            background-color: var(--dark);
            color: white;
            padding: 0.3rem 0.8rem;
            border-radius: 5px;
            font-size: 0.8rem;
            opacity: 0;
            transition: all 0.3s ease;
            pointer-events: none;
            white-space: nowrap;
        }
        
        .contact-option:hover::after {
            opacity: 1;
            bottom: -35px;
        }
        
        .email { background-color: #D44638; }
        .linkedin { background-color: #0077B5; }
        .github { background-color: #333; }
        .fiverr { background-color: #1DBF73; }
        .upwork { background-color: #14A800; }
        
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-15px); }
            100% { transform: translateY(0px); }
        }
        
        @keyframes ripple {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }
        
        .particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }
        
        .particle {
            position: absolute;
            background: linear-gradient(to right, rgba(108, 99, 255, 0.2), rgba(99, 210, 255, 0.2));
            border-radius: 50%;
            animation: float-particle 15s infinite linear;
        }
        
        @keyframes float-particle {
            0% {
                transform: translateY(0) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(-1000px) rotate(720deg);
                opacity: 0;
            }
        }
        
        @media (max-width: 768px) {
            .contact-container {
                flex-direction: column;
            }
            
            .illustration-section {
                height: auto;
                margin-bottom: 2rem;
            }
            
            .developer-avatar {
                width: 200px;
                height: 200px;
            }
            
            .avatar-content i {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <div class="gradient-bg"></div>
    <div class="particles" id="particles"></div>
    
    <div class="contact-container">
        <div class="illustration-section">
            <div class="developer-avatar">
                <div class="avatar-content">
                    <i class="fas fa-code"></i>
                </div>
            </div>
            <div class="greeting">
                <h2>Let's Build Something Amazing</h2>
                <p>I'm passionate about creating beautiful, functional web applications with React. Whether you have a project in mind or just want to connect, I'd love to hear from you!</p>
            </div>
        </div>
        
        <div class="form-section">
            <h2>Get In Touch</h2>
            <form id="contactForm">
                <div class="form-group">
                    <input type="text" class="form-control" id="name" required placeholder=" ">
                    <label for="name" class="form-label">Your Name</label>
                    <i class="fas fa-check validation-icon"></i>
                </div>
                
                <div class="form-group">
                    <input type="email" class="form-control" id="email" required placeholder=" ">
                    <label for="email" class="form-label">Email Address</label>
                    <i class="fas fa-check validation-icon"></i>
                </div>
                
                <div class="form-group">
                    <input type="text" class="form-control" id="subject" required placeholder=" ">
                    <label for="subject" class="form-label">Subject</label>
                    <i class="fas fa-check validation-icon"></i>
                </div>
                
                <div class="form-group">
                    <textarea class="form-control" id="message" required placeholder=" "></textarea>
                    <label for="message" class="form-label">Your Message</label>
                </div>
                
                <button type="submit" class="submit-btn" id="submitBtn">
                    Send Message
                </button>
            </form>
            
            <div class="contact-options">
                <a href="mailto:contact@reactdev.com" class="contact-option email" data-tooltip="Email">
                    <i class="fas fa-envelope"></i>
                </a>
                <a href="https://linkedin.com" target="_blank" class="contact-option linkedin" data-tooltip="LinkedIn">
                    <i class="fab fa-linkedin-in"></i>
                </a>
                <a href="https://github.com" target="_blank" class="contact-option github" data-tooltip="GitHub">
                    <i class="fab fa-github"></i>
                </a>
                <a href="https://fiverr.com" target="_blank" class="contact-option fiverr" data-tooltip="Fiverr">
                    <i class="fab fa-fonticons-fi"></i>
                </a>
                <a href="https://upwork.com" target="_blank" class="contact-option upwork" data-tooltip="Upwork">
                    <i class="fab fa-upwork"></i>
                </a>
            </div>
        </div>
    </div>

    <script>
        // Ripple effect for submit button
        document.getElementById('submitBtn').addEventListener('click', function(e) {
            const btn = e.currentTarget;
            const rect = btn.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            
            const ripple = document.createElement('span');
            ripple.classList.add('ripple');
            ripple.style.left = `${x}px`;
            ripple.style.top = `${y}px`;
            
            btn.appendChild(ripple);
            
            setTimeout(() => {
                ripple.remove();
            }, 600);
        });
        
        // Form submission
        document.getElementById('contactForm').addEventListener('submit', function(e) {
            e.preventDefault();
            alert('Thank you for your message! I will get back to you soon.');
            this.reset();
        });
        
        // Create particles
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 15;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                
                // Random size between 50px and 150px
                const size = Math.random() * 100 + 50;
                particle.style.width = `${size}px`;
                particle.style.height = `${size}px`;
                
                // Random position
                particle.style.left = `${Math.random() * 100}%`;
                particle.style.top = `${Math.random() * 100 + 100}%`;
                
                // Random animation duration between 10s and 20s
                const duration = Math.random() * 10 + 10;
                particle.style.animationDuration = `${duration}s`;
                
                // Random delay
                particle.style.animationDelay = `${Math.random() * 5}s`;
                
                particlesContainer.appendChild(particle);
            }
        }
        
        // Initialize particles when page loads
        window.addEventListener('load', createParticles);
    </script>
</body>
</html>